# wildfire_bringup è un pacchetto ament_python.
# L'installazione di launch/ e config/ è gestita da setup.py.
# Questo file CMakeLists.txt è vuoto per evitare errori di colcon.
cmake_minimum_required(VERSION 3.8)
project(wildfire_bringup)
